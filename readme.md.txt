Process to Run app:

Step 1 : To start the project first need to start the express server through the nodejs
         and open the port (4001).

Step 2 : for start express server open cmd.

Step 3 : Go inside the project folder type the command: node getDataServer.js

Step 4 : open the browser and type the url: localhost:4001/	